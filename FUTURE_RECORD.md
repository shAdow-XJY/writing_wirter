### waiting for better
##### high level
1. 字体包:更换为一个对中文显示更友好的字体包，这个的"门"显示错误了，其它不做追究记录。
2. 通过复制进去的文字，需要键盘再次触发输入字符，也就是随便写一个字符删除后，之前复制的文字才会触发保存，待修复。
---

##### low level
1. 字体包切换（本地系统字体供选择）
2. 字体样式选择
3. webDAV上传到网盘的内容也可以先加密再上传，下载下来再解密
4. IOBase频繁的对内容进行读写，中间考虑添加缓存区，减少IO操作和提高性能
5. 设定集里的设定重命名后，可以选择将章节里的内容中“设定”文字也同时替换
6. 设定集中的设定，可以添加一个别名

##### waiting release
1. 将打包后的应用程序在GitHub上release，然后将构建时的代码版本分出一个tag(就像Flutter一样)